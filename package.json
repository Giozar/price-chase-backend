{
  "name": "price-chase-backend",
  "version": "1.0.0",
  "description": "Backend para Price Chase con Node, Express y TypeScript",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "dev": "node --env-file=.env --watch --import tsx src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["node", "express", "typescript", "backend"],
  "author": "Tu nombre",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.6.2",
    "tsx": "^4.19.1"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
